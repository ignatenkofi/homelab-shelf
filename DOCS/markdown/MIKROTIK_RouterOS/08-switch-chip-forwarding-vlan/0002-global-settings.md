## Global Settings 

413 

The CRS switch chip is configurable from the `/interface ethernet switch` console menu. 

Sub-menu: `/interface ethernet switch` 

**==> picture [507 x 634] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>name  (string  Name of the switch.<br>value;<br>Default:  switc<br>h1 )<br>bridge-type  (c The bridge type defines which VLAN tag is used as Lookup-VID. Lookup-VID serves as the VLAN key for all VLAN-based lookups.<br>ustomer-vid-<br>used-as-<br>lookup-vid |<br>service-vid-<br>used-as-<br>lookup-vid;<br>Default:  custo<br>mer-vid-used-<br>as-lookup-vid )<br>mac-level- Globally enables or disables MAC level isolation. Once enabled, the switch will check the source and destination MAC address<br>isolation  (yes  entries and their isolation-profile from the unicast forwarding table. By default, the switch will learn MAC addresses and place<br>| no; Default: them into a promiscuous isolation profile. Other isolation profiles can be used when creating static unicast entries. If the source or<br>yes ) destination MAC address is located on a promiscuous isolation profile, the packet is forwarded. If both source and destination<br>MAC addresses are located on the same community1 or community2 isolation profile, the packet is forwarded. The packet is<br>dropped when the source and destination MAC address isolation profile is isolated , or when the source and destination MAC<br>address isolation profiles are from different communities (e.g. source MAC address is community1 and destination MAC address is<br>community2 ). When MAC level isolation is globally disabled, the isolation is bypassed.<br>use-svid-in- Whether to use service VLAN ID for 1:1 VLAN switching lookup.<br>one2one-<br>vlan-lookup  (y<br>es | no;<br>Default: no )<br>use-cvid-in- Whether to use customer VLAN ID for 1:1 VLAN switching lookup.<br>one2one-<br>vlan-lookup  (y<br>es | no;<br>Default: yes )<br>multicast- Lookup mode for IPv4 multicast bridging.<br>lookup-mode<br>dst-mac-and-vid-always - For all packet types lookup key is the destination MAC and VLAN ID.<br>(dst-ip-and- dst-ip-and-vid-for-ipv4 - For IPv4 packets lookup key is the destination IP and VLAN ID. For other packet types, the lookup key<br>vid-for-ipv4 |  is the destination MAC and VLAN ID.<br>dst-mac-and-<br>vid-always;<br>Default: dst-ip-<br>and-vid-for-<br>ipv4 )<br>unicast-fdb- Timeout for Unicast FDB entries.<br>timeout  (time<br>interval;<br>Default: 5m )<br>override- Enable or disable to override existing entry which has the lowest aging value when UFDB is full.<br>existing-<br>when-ufdb-full<br>(yes | no;<br>Default: no )<br>**----- End of picture text -----**<br>


414 

**==> picture [507 x 130] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>drop-if-no-vlan-assignment-on-ports  (ports;  Ports which drop frames if no MAC-based, Protocol-based VLAN assignment or Ingress VLAN<br>Default: none ) Translation is applied.<br>drop-if-invalid-or-src-port- Ports that drop invalid and other port VLAN ID frames.<br>-not-member-of-vlan-on-ports<br>(ports; Default: none )<br>unknown-vlan-lookup-mode  (ivl | svl; Default: svl ) Lookup and learning mode for packets with invalid VLAN.<br>forward-unknown-vlan  (yes | no; Default: yes ) Whether to allow forwarding VLANs that are not members of the VLAN table.<br>**----- End of picture text -----**<br>


**==> picture [507 x 122] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>bypass-vlan-ingress-filter-for  (protocols;  Protocols that are excluded from Ingress VLAN filtering. These protocols are not dropped if they have<br>Default: none ) invalid VLAN. (arp, dhcpv4, dhcpv6,<br>eapol, igmp, mld, nd, pppoe-discovery, ripv1)<br>bypass-ingress-port-policing-for  (protocols;  Protocols that are excluded from Ingress Port Policing. (arp, dhcpv4, dhcpv6, eapol, igmp, mld, nd,<br>Default: none ) pppoe-discovery, ripv1)<br>bypass-l2-security-check-filter-for  (protocols Protocols that are excluded from Policy rule security check. (arp, dhcpv4, dhcpv6, eapol, igmp, mld,<br>; Default: none ) nd, pppoe-discovery, ripv1)<br>**----- End of picture text -----**<br>


**==> picture [507 x 340] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>ingress-mirror0  (port | trunk,format; Default: no The first ingress mirroring analyzer port or trunk and mirroring format:<br>ne,modified )<br>analyzer-configured - The packet is the same as the packet to the destination. VLAN format is<br>modified based on the VLAN configurations of the analyzer port.<br>modified - The packet is the same as the packet to the destination. VLAN format is modified<br>based on the VLAN configurations of the egress port.<br>original - Traffic is mirrored without any change to the original incoming packet format. But the<br>service VLAN tag is stripped in the edge port.<br>ingress-mirror1  (port | trunk,format; Default: no The second ingress mirroring analyzer port or trunk and mirroring format:<br>ne,modified )<br>analyzer-configured - The packet is the same as the packet to the destination. VLAN format is<br>modified based on the VLAN configurations of the analyzer port.<br>modified - The packet is the same as the packet to the destination. VLAN format is modified<br>based on the VLAN configurations of the egress port.<br>original - Traffic is mirrored without any change to the original incoming packet format. But the<br>service VLAN tag is stripped in the edge port.<br>ingress-mirror-ratio  (1/32768..1/1; Default: 1/1 ) The proportion of ingress mirrored packets compared to all packets.<br>egress-mirror0  (port | trunk,format; Default: no The first egress mirroring analyzer port or trunk and mirroring format:<br>ne,modified )<br>analyzer-configured - The packet is the same as the packet to the destination. VLAN format is<br>modified based on the VLAN configurations of the analyzer port.<br>modified - The packet is the same as the packet to the destination. VLAN format is modified<br>based on the VLAN configurations of the egress port.<br>original - Traffic is mirrored without any change to the original incoming packet format. But the<br>service VLAN tag is stripped in the edge port.<br>**----- End of picture text -----**<br>


415 

**==> picture [507 x 264] intentionally omitted <==**

**----- Start of picture text -----**<br>
egress-mirror1  (port | trunk,format; Default: no The second egress mirroring analyzer port or trunk and mirroring format:<br>ne,modified )<br>analyzer-configured - The packet is the same as the packet to the destination. VLAN format is<br>modified based on the VLAN configurations of the analyzer port.<br>modified - The packet is the same as the packet to the destination. VLAN format is modified<br>based on the VLAN configurations of the egress port.<br>original - Traffic is mirrored without any change to the original incoming packet format. But the<br>service VLAN tag is stripped in the edge port.<br>egress-mirror-ratio  (1/32768..1/1; Default: 1/1 ) Proportion of egress mirrored packets compared to all packets.<br>mirror-egress-if-ingress-mirrored  (yes | no;  When a packet is applied to both ingress and egress mirroring, only ingress mirroring is performed<br>Default: no ) on the packet, if this setting is disabled. If this<br>setting is enabled both mirroring types are applied.<br>mirror-tx-on-mirror-port  (yes | no; Default: no )<br>mirrored-packet-qos-priority  (0..7; Default: 0 ) Remarked priority in mirrored packets.<br>mirrored-packet-drop-precedence  (drop |  Remarked drop precedence in mirrored packets. This QoS attribute is used for mirrored packet<br>green | red | yellow; Default: green ) enqueuing or dropping.<br>fdb-uses  (mirror0 | mirror1; Default: mirror0 ) Analyzer port used for FDB-based mirroring.<br>vlan-uses  (mirror0 | mirror1; Default: mirror0 ) Analyzer port used for VLAN-based mirroring.<br>**----- End of picture text -----**<br>
