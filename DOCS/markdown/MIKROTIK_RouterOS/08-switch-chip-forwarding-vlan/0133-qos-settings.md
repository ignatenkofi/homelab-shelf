## QoS Settings 

Sub-menu: `/interface/ethernet/switch/qos/settings` 

**==> picture [516 x 480] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>mirror- Maximum amount of packet buffers for mirrored traffic (% of the total buffer memory).<br>buffers  (perc<br>ent: 1..90;<br>Default:  10 )<br>mirror-profile The name of the QoS profile to assign to the mirrored packets (see  /in/eth/sw/qos/profile ).<br>(name;<br>Default:  defa<br>ult )<br>multicast- Maximum amount of packet buffers for multicast/broadcast traffic (% of the total buffer memory).<br>buffers  (perc<br>ent: 1..90;<br>Default:  10 )<br>shared- Maximum amount of packet buffers that are shared between ports (% of the total buffer memory). Setting it to 0 disables buffer sharing.<br>buffers  (perc The remaining buffer memory is split between the ports.<br>ent: 0..90;<br>Default:  40 )<br>shared- Restricts shared buffer usage for specific traffic colors only.<br>buffers-color<br>(all | green-<br>only | yellow-<br>and-green;<br>Default:  all )<br>shared- If the device supports multiple shared buffer pools, these settings allows adjusting the size of each pool (% of the shared buffer memory,<br>pool0 ..  where 100% means all shared buffers allocated by the  shared-buffers  setting). For example, if shared-buffers=40 and shared-pool0=50,<br>shared- the shared pool #0 (the first one) receives 20% of the total buffer memory (50% of 40% or "0.5 * 0.4 = 0.2"). Auto mode tries to equally<br>pool7  (perce allocate available resources between pools that uses auto setting, and provides at least a minimum of 10% of the total shared buffer size<br>nt: 0..100;  if the sum of other manually configured pools are exceeded. The default setting (auto).<br>Default:  auto )<br>treat-yellow- For devices that support only two-color traffic marking (red/green). This setting allows using the same QoS profiles for the devices with<br>as  (green |  two- and three-color traffic marking.<br>red; Default:<br>red )<br>wred- A relative amount of packets above a shared queue cap (" queueX-shared-packet-cap " or " queueX-shared-byte-cap ") where<br>threshold  (lo random drops take place. This threshold is applied only to queues with enabled Weighed Random Early Detection ( wred=yes ) that use<br>w | medium  shared buffers ( use-shared-buffers=yes) . The higher the queue buffer fill level, the higher the packet drop chance. The low threshold<br>| high; Defau means the random tail drop starts later; the high - sooner.<br>lt: medium )<br>**----- End of picture text -----**<br>
