## Compatibility Address 

**==> picture [516 x 104] intentionally omitted <==**

**----- Start of picture text -----**<br>
Address Description<br>IPv4  used by dual-stack nodes that are communicating with IPv6 over an IPv4 infrastructure. When the IPv4-compatible address is used as an<br>compatibl IPv6 destination, IPv6 traffic is automatically encapsulated with an IPv4 header and sent to the destination by using the IPv4 infrastructure.<br>e address The address is written in the following format  ::w.x.y.z , where w.x.y.z is the dotted decimal representation of a public IPv4 address.<br>IPv4  used to represent an IPv4-only node to an IPv6 node. It is used only for internal representation. The IPv4-mapped address is never used as<br>mapped  a source or destination address for an IPv6 packet. The IPv6 protocol does not support the use of IPv4-mapped addresses. The address is<br>address written in the following format:  ::ffff:w.x.y.z , where  w.x.y.z  is the dotted-decimal representation of a public IPv4 address.<br>**----- End of picture text -----**<br>
