## `/routing/pimsm/instance` 

The instance menu defines the main PIM-SM settings. The instance is then used for all other PIM-related configurations like interface-template, static RP, and Bootstrap Router. 

**==> picture [516 x 326] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>afi  (ipv4 | ipv6; Default: ipv4 ) Specifies address family for PIM.<br>bsm-forward-back  (yes | no;  Currently not implemented.<br>Default: )<br>crp-advertise-contained  (yes | no; Currently not implemented.<br>Default: )<br>name  (text; Default: ) Name of the instance.<br>rp-hash-mask-length  (integer: 0.. The hash mask allows changing how many groups to map to one of the matching RPs.<br>4294967295; Default:  30  (IPv4),<br>or  126  (IPv6))<br>rp-static-override  (yes | no;  Changes the selection priority for static RP. When disabled, the bootstrap RP set has a higher priority. When<br>Default: no ) enabled, static RP has a higher priority.<br>ssm-range (IPv4 | IPv6; Default: ) Currently not implemented.<br>switch-to-spt  (yes | no; Default: y Whether to switch to Shortest Path Tree (SPT) if multicast data bandwidth threshold is reached. The router will not<br>es ) proceed from protocol phase one (register encapsulation) to native multicast traffic flow if this option is disabled. It is<br>recommended to enable this option.<br>switch-to-spt-bytes  (integer: 0.. Multicast data bandwidth threshold. Switching to Shortest Path Tree (SPT) happens if this threshold is reached in<br>4294967295; Default:  0 ) the specified time interval. If a value of 0 is configured, switching will happen immediately.<br>switch-to-spt-interval (time;  Time interval in which to account for multicast data bandwidth, used in conjunction with  switch-to-spt-bytes  to<br>Default: ) determine if the switching threshold is reached.<br>vrf (name; Default: main) Name of the VRF.<br>**----- End of picture text -----**<br>
