## General 

```
/ip/proxy
```

**==> picture [501 x 400] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>always-from-cache  (yes ignore client refresh requests if the content is considered fresh<br>| no; Default: no )<br>anonymous  (yes | no;  If not set, the IP address of the client would be passed X-Forwarded-For header (could be accessed using<br>Default: no ) HTTP_X_FORWARDED_FOR environment variable in remote servers)<br>cache-administrator  (str Administrator's e-mail displayed on proxy error page<br>ing; Default: webmaster )<br>cache-hit-dscp  (integer: Automatically mark cache hit with the provided DSCP value<br>0..63; Default: 4 )<br>cache-on-disk  (yes | no; Whether to store cache on disk<br>Default: no )<br>cache-path  (string;  A path where the cache will be stored, when cache-on-disk is enabled.<br>Default: web-proxy )<br>max-cache-object-size  ( Specifies the maximal cache object size, measured in kilobytes<br>integer: 0..4294967295<br>[KiB]; Default: 2048KiB )<br>max-cache-size  (none  Specifies the maximal cache size, measured in kilobytes<br>| unlimited | integer: 0..<br>4294967295[KiB];<br>Default: unlimited )<br>max-client-connections  ( Maximal number of connections accepted from clients (any further connections will be rejected)<br>integer: Dynamic ;<br>Default: 600 )<br>max-fresh-time  (time;  Maximal time to store a cached object. The validity period of an object is usually defined by the object itself, but in case it<br>Default: 3d ) is set too high, you can override the maximal value<br>max-server- Maximal number of connections made to servers (any further connections from clients will be put on hold until some<br>connections  (integer:  server connections will terminate)<br>Dynamic ; Default: 600 )<br>**----- End of picture text -----**<br>


934 

parent-proxy (Ip4 | ip6; IP address and port of another HTTP proxy to redirect all requests to. If set to 0.0.0.0 parent proxy is not used. Default: 0.0.0.0 ) 

**==> picture [501 x 151] intentionally omitted <==**

**----- Start of picture text -----**<br>
parent-proxy-port  (integ Port that parent proxy is listening on.<br>er: 0..65535; Default: 0 )<br>port  (integer: 0..65535;  TCP port the proxy server will be listening on. This port has to be specified on all clients that want to use the server as an<br>Default: 8080 ) HTTP proxy. A transparent (with zero configuration for clients) proxy setup can be made by redirecting HTTP requests to<br>this port in the IP firewall using the destination NAT feature<br>serialize-connections  (y Do not make multiple connections to the server for multiple client connections, if possible (i.e. server supports persistent<br>es | no; Default: no ) HTTP connections). Clients will be served on the FIFO principle; the next client is processed when the response transfer<br>to the previous one is completed. If a client is idle for too long (max 5 seconds by default), it will give up waiting and open<br>another connection to the server<br>src-address  (Ip4 | Ip6;  A proxy will use a specified address when connecting to the parent proxy or website. If set to 0.0.0.0 then the appropriate<br>Default: 0.0.0.0 ) IP address will be taken from the routing table.<br>**----- End of picture text -----**<br>
