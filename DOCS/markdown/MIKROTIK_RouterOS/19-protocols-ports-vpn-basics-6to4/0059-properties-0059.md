## Properties 

**==> picture [516 x 391] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>action  (discard | encrypt |  Specifies what to do with the packet matched by the policy.<br>none; Default:  encrypt )<br>none - pass the packet unchanged.<br>discard - drop the packet.<br>encrypt - apply transformations specified in this policy and it's SA.<br>comment  (string; Default: ) Short description of the policy.<br>disabled  (yes | no; Default:  no ) Whether a policy is used to match packets.<br>dst-address  (IP/IPv6 prefix;  Destination address to be matched in packets. Applicable when tunnel mode (tunnel=yes) or template (template=yes)<br>Default:  0.0.0.0/32 ) is used.<br>dst-port  (integer:0..65535 |  Destination port to be matched in packets. If set to any all ports will be matched.<br>any; Default:  any )<br>group  (string; Default:  default ) Name of the policy group to which this template is assigned.<br>ipsec-protocols  (ah | esp;  Specifies what combination of Authentication Header and Encapsulating Security Payload protocols you want to apply<br>Default:  esp ) to matched traffic.<br>level  (require | unique | use;  Specifies what to do if some of the SAs for this policy cannot be found:<br>Default:  require )<br>use - skip this transform, do not drop the packet, and do not acquire SA from IKE daemon;<br>require - drop the packet and acquire SA;<br>unique - drop the packet and acquire a unique SA that is only used with this particular policy. It is used in setups<br>where multiple clients can sit behind one public IP address (clients behind NAT).<br>peer  (string; Default: ) Name of the peer on which the policy applies.<br>proposal  (string; Default:  defa Name of the proposal template that will be sent by IKE daemon to establish SAs for this policy.<br>ult )<br>protocol  (all | egp | ggp| icmp |  IP packet protocol to match.<br>igmp | ...; Default:  all )<br>**----- End of picture text -----**<br>


1194 

src-address (ip/ipv6 prefix; Source address to be matched in packets. Applicable when tunnel mode (tunnel=yes) or template (template=yes) is Default: 0.0.0.0/32 ) used. src-port (any | integer:0.. Source port to be matched in packets. If set to any all ports will be matched. 65535; Default: any ) template (yes | no; Default: no ) Creates a template and assigns it to a specified policy group. Following parameters are used by template: group - name of the policy group to which this template is assigned; src-address, dst-address - Requested subnet must match in both directions(for example 0.0.0.0/0 to allow all); protocol - protocol to match, if set to all, then any protocol is accepted; proposal - SA parameters used for this template; level - useful when unique is required in setups with multiple clients behind NAT. tunnel (yes | no; Default: no ) Specifies whether to use tunnel mode.
