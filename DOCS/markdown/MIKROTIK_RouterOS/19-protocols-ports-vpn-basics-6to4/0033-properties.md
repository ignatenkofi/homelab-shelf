## Properties 

**==> picture [516 x 379] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>allow-fast-path  (y Whether to allow FastPath processing. Must be disabled if IPsec tunneling is used.<br>es | no; Default:  y<br>es )<br>clamp-tcp-mss  (y Controls whether to change MSS size for received TCP SYN packets. When enabled, a router will change the MSS size for<br>es | no; Default:  y received TCP SYN packets if the current MSS size exceeds the tunnel interface MTU (taking into account the TCP/IP overhead).<br>es ) The received encapsulated packet will still contain the original MSS, and only after decapsulation the MSS is changed.<br>comment  (string;  Short description of the tunnel.<br>Default: )<br>disabled  (yes | no Enables/disables tunnel.<br>; Default:  no )<br>dont-fragment  (in Whether to include DF bit in related packets:<br>herit | no;<br>Default:  no ) no - fragment if needed, inherit - use Dont Fragment flag of original packet.<br>(Without Dont Fragment: inherit - packet may be fragmented).<br>dscp  (inherit |  Set dscp value in Gre header to a fixed value or inherit from dscp value taken from tunnelled traffic<br>integer [0-63];<br>Default: )<br>ipsec-secret  (string When secret is specified, router adds dynamic IPsec peer to remote-address with pre-shared key and policy (by default phase2<br>; Default: ) uses sha1/aes128cbc).<br>keepalive  (integer Tunnel keepalive parameter sets the time interval in which the tunnel running flag will remain even if the remote end of tunnel goes<br>[/time],integer 0.. down. If configured time,retries fail, interface running flag is removed. Parameters are written in following format:  KeepaliveInter<br>4294967295;  val,KeepaliveRetries  where KeepaliveInterval is time interval and KeepaliveRetries - number of retry attempts. By default<br>Default:  10s,10 ) keepalive is set to 10 seconds and 10 retries.<br>l2mtu  (integer [0.. Layer2 Maximum transmission unit.<br>65536]; Default:  6<br>5535 )<br>**----- End of picture text -----**<br>


1183 

local-address (IP; IP address that will be used for local tunnel end. If set to 0.0.0.0 then IP address of outgoing interface will be used. Default: 0.0.0.0 ) mtu (integer [0.. Layer3 Maximum transmission unit. 65536]; Default: 1 476 ) name (string; Name of the tunnel. Default: ) remote-address (IP IP address of remote tunnel end. ; Default: )
