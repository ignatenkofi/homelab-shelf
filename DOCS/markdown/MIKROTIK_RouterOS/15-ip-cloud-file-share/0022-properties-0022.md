## Properties 

**==> picture [506 x 357] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>add-default-route  (yes Whether to add default IPv6 route after a client connects.<br>| no; Default: no )<br>allow-reconfigure  (yes Allows to receive Reconfigure (forcerenew) message from DHCP server.<br>| no; Default:  no )t<br>default-route-tables  (t List of routing tables to which default route must be added. Table name can be proceeded with ":x" where x would be the<br>able:distance;  distance for the route to be installed with.<br>Default: main)<br>comment  (string;  Short description of the client<br>Default: )<br>disabled  (yes | no;<br>Default: no )<br>interface  (string;  The interface on which the DHCPv6 client will be running.<br>Default: )<br>pool-name  (string;  Name of the IPv6 pool in which received IPv6 prefix will be added<br>Default: )<br>pool-prefix-length  (stri Prefix length parameter that will be set for IPv6 pool in which received IPv6 prefix is added. Prefix length must be greater or<br>ng; Default: ) equal as the length of received prefix, otherwise, prefix-length will be set to received prefix length + 8 bits.<br>prefix-address-lists  (st Names of the firewall address lists to which received prefix will be added.<br>ring; Default: )<br>prefix-hint  (string;  Include a preferred prefix length.<br>Default: )<br>request  (prefix,  to choose if the DHCPv6 request will ask for the address or the IPv6 prefix, or both.<br>address; Default: )<br>**----- End of picture text -----**<br>


888 

**==> picture [506 x 277] intentionally omitted <==**

**----- Start of picture text -----**<br>
script  (string; Default:  Run this script on the DHCP-client status change. Available variables:<br>)<br>pd-valid - if the prefix is acquired by the client;<br>pd-prefix - the prefix acquired by the client if any;<br>na-valid - if the address is acquired by the client;<br>na-address - the address acquired by the client if any.<br>options - array of received options (only ROSv7)<br>use-peer-dns  (yes |  Whether to accept the DNS settings advertised by the IPv6 DHCP Server.<br>no; Default: yes )<br>custom-duid  (hex  Allow to specify custom DUID.<br>string; Default: )<br>use-interface-duid  (ye According to RFC DHCPv6 client generate DUID based on the router first interface MAC address, not the interface on which<br>s | no; Default: no ) client is configured. With this option enabled you will override this requirement and MAC address from "interface" specified<br>on client will be used.<br>validate-server-duid  ( Allow to ignore incorrectly formed DUID provided by DHCPv6 server. Still checks that minimal DUID length is correct.<br>yes | no; Default:  yes )<br>custom-iapd-id  (hex  Allow to specify custom IAPD ID.<br>string; Default: )<br>custom-iana-id  (hex  Allow to specify custom IANA ID.<br>string; Default: )<br>**----- End of picture text -----**<br>
