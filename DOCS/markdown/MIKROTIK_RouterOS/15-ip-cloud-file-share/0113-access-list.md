## Access List 

```
/ip socks access
```

In the SOCKS access list, you can add rules which will control access to the SOCKS server. This list is similar to firewall lists. 

**==> picture [393 x 137] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>action (allow | deny; default: allow) allow - allow packets, matching this rule, to be forwarded for further processing<br>deny - deny access for packets, matching this rule<br>dst-address (IP address/netmask) destination (server's) address<br>dst-port (port) destination TCP port<br>src-address (IP address/netmask) source (client's) address for a packet<br>src-port (port)  source TCP port<br>**----- End of picture text -----**<br>
