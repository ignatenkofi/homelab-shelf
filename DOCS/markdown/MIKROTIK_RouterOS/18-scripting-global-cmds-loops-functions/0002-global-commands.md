## Global commands 

Every global command should start with the ":" token, otherwise, it will be treated as a variable. 

**==> picture [509 x 580] intentionally omitted <==**

**----- Start of picture text -----**<br>
Command Syntax Description Example<br>/ go to the root menu<br>.. go back by one menu level<br>? list all available menu commands and brief descriptions<br>global :global <var>  define a global variable :global myVar "something"; :put<br>[<value>] $myVar;<br>local :local <var>  define the local variable { :local myLocalVar "I am<br>[<value>] local"; :put $myVar; }<br>beep :beep frequency= beep built-in speaker<br>[num] length=<br>[num]<br>convert :convert from= Converts specified value from one format to another. By default uses an  :put [:convert 001 to=hex ]<br>[arg] to=[arg] automatically parsed value, if the "from" format is not specified (for example,<br>"001" becomes "1", "10.1" becomes "10.0.0.1", etc.). 31<br>from  specifies the format of the value - base32, base64, byte-array, hex, num,  :put [:convert [/ip dhcp-client<br>raw, url. /option/get hostname raw-value]<br>from=hex to=raw ]<br>to  specifies the format of the output value - base32, base64, bit-array-lsb, bit-<br>array-msb, byte-array, hex, num, raw, url. MikroTik<br>transform  to transform values - lc (transforms value to be in lowercases), uc  :put [convert transform=lc<br>(uppercases), lcfirst (first value to lowercase), ucfirst (first value to uppercase),  "AAA"]<br>crlf, ed25519-private-to-x25519-private, none, rot 13, x25519-private-to-x25519-<br>public, ed25519-private-to-ed25519-public, ed25519-public-to-x25519-public,  aaa<br>md5, reverse (reverses text), sha512.<br>delay :delay <time> do nothing for a given period of time<br>environment :environment  print initialized variable information :global myVar true; :<br>print <start> environment print;<br>error :error <output> Generate console error and stop executing the script<br>execute :execute  Execute the script in the background. The result can be written in the file by<br>{<br><expression> setting a "file" parameter or printed to the CLI by setting "as-string".<br>:local j [:execute {<br>When using the "as-string" parameter executed script is blocked (not executed  /interface print follow<br>in the background). where [:log info<br>~Sname~]}];<br>Executed script can not be larger than 64kB :delay 10s;<br>:onerror e {/system<br>script job remove $j}<br>}<br>find :find <arg>  return position of a substring or array element :put [:find "abc" "a" -1];<br><arg> <start><br>grep :grep script= Command ":grep" executes "script" part of a command in terminal and provides  :grep script="/interface print"<br>[str] pattern= just the output which matches "pattern". Additional options "after" and "before"  pattern="ether" after=1<br>[expression] afte allow to print out specific number of lines of script output which are available  before=1 filename=results.txt<br>r=[num] before= also after/before matched pattern. Results can be directly saved into a file with<br>[num] filename= "filename" parameter.<br>[str]<br>**----- End of picture text -----**<br>


1101 

**==> picture [509 x 685] intentionally omitted <==**

**----- Start of picture text -----**<br>
jobname :jobname return current script name<br>Limit script execution to single<br>instance<br>:if ([/system script job<br>print count-only as-value<br>where script=[:jobname] ]<br>> 1) do={<br>  :error "script instance<br>already running"<br>  }<br>len :len <expression> return string length or array element count :put [:len "length=8"];<br>log :log <topic>  write a message to the system log. Available topics are "debug, error,  :log info "Hello from script";<br><message> info and warning"<br>onerror :onerror  The command used to catch errors and get error details. The  do={...}  block is   :onerror errorName in={ :error<br><var_name> in= executed, when  in={...}  block has an error,  and error details are written in  "failure" } do={ :put "Critical<br>{<command>} do= <var_name> variable.  $errorName" }<br>{<expression>}<br>Parameter order is important. The "error" parameter must be set before "do"<br>block, otherwise do block will not see the local variable.<br>:onerror will return false (if there is no error) and true (if there is an error) unless<br>otherwise specified (with commands such as :return or :error), so it can be used<br>in  :if  condition statement scripts.<br>parse :parse  parse the string and return parsed console commands. Can be used as a  :global myFunc [:parse ":put<br><expression> function. hello!"];<br>$myFunc;<br>pick :pick <var>  return range of elements or substring. If the count is not specified, will return<br><start> [<end>] only one element from an array.<br>[admin@MikroTik] > :put [:<br>var - value to pick elements from<br>pick "abcde" 1 3]<br>start - element to start picking from (the first element index is 0)<br>bc<br>end - terminating index (element at this index is not included)<br>put :put <expression> put the supplied argument into the console :put "Hello world"<br>range :range <var>  creates an array from the specified range :put [:range 2 8]<br><var> 2;3;4;5;6;7;8<br>resolve :resolve <arg>  return the IP address of the given DNS name :put [:resolve "www.mikrotik.com<br>[<domain-name>] "];<br>[<server>] domain-name - DNS name that needs to be resolved;<br>[<server-port>] server - specific server that will be used to resolve DNS name (returned  :put [:resolve domain-name="www.<br>[<type>] results will not be cached); mikrotik.com"];<br>server-port - server port;<br>type - any/any6/ipv4/ipv6: :put [:resolve domain-name="www.<br>any - first tries to resolve ipv4, if fails tries ipv6; mikrotik.com" server=192.<br>any6 - first tries to resolve ipv6, if fails tries ipv4; 168.88.1 port=53];<br>ipv4 - tries to resolve only ipv4;<br>:put [:resolve domain-name="www.<br>ipv6 - tries to resolve only ipv6<br>mikrotik.com" type=ipv6];<br>retry :onerror e {:retry  Try to execute the given command "max" amount of times with a given "delay"<br>:onerror e {:retry<br>command=<expr>  in seconds between tries. On failure, execute the command in the do={} block.<br>command={abc} delay=1<br>delay=[num] max=<br>max=2} do={:put "got<br>[num]} do={<expr>}<br>error"}<br>got error<br>typeof :typeof <var> the return data type of variable :put [:typeof 4];<br>**----- End of picture text -----**<br>


1102 

**==> picture [509 x 552] intentionally omitted <==**

**----- Start of picture text -----**<br>
rndnum :rndnum from= random number generator :put [:rndnum from=1 to=99];<br>[num] to=[num]<br>rndstr :rndstr from= Random string generator. :put [:rndstr from="abcdef%^&"<br>[str] length= length=33];<br>[num] from  specifies characters to construct the string from and defaults to all ASCII<br>letters and numerals.<br>length  specifies the length of the string to create and defaults to 16.<br>set :set <var>  assign value to a declared variable. :global a; :set a true;<br>[<value>]<br>serialize :serialize [<valu Serialize specified value/array to JSON or dsv (delimiter separated values)<br>e>] to=[arg] format.<br>:put [:serialize value=a,<br>value  specifues which values to process.<br>b,c<br>to  specifies the format - json, dsv to=json]<br>["a","b","c"]<br>delimiter  sets the "separator".<br>:local test {a=(1,2,3);b=<br>order  specifies the order for variables. (4,5,6);c=(7,"text",9)}; :<br>put [ :serialize to=dsv<br>options  specifies additional options:<br>delimiter=";" value=$test<br>json.pretty  - makes the JSON output more visually appealing; order=("c","a","b") ]<br>json.no-string-conversion - prevents implict conversions from console  c;a;b<br>string type to json number type; 7;1;4<br>dsv.wrap-strings - wraps string values inside quatation marks; text;2;5<br>dsv.ignore-size - if array values have different sizes, e.g.  a=(1,2);b= 9;3;6<br>(3,4);c=(5,6,7) , this option will work around  array size<br>mismatch  error and set "empty" values in those slots. :global var ({ "string"="<br>dsv.remap - merges array of dictionaries into a single dictionary (useful  1234"; "number"=1234 });:<br>when working with " print as-value ") put [ :serialize to=json<br>value=$var ]<br>file-name  enables the option to generate command's output into a file (available  {"number":1234,"string":<br>for download in the "/files" section). 1234.000000}<br>:put [ :serialize to=json<br>value=$var options=json.<br>no-string-conversion  ]<br>{"number":1234,"string":"<br>1234"}<br>:put [:serialize to=dsv<br>options=dsv.remap<br>delimiter="#" [/ip/address<br>/print as-value]]<br>.<br>id#address#comment#interfa<br>ce#network<br>*1#192.168.88.1<br>/24#defconf#bridge#192.<br>168.88.0<br>*2#192.168.69.190<br>/24##ether1#192.168.69.0<br>**----- End of picture text -----**<br>


1103 

**==> picture [509 x 687] intentionally omitted <==**

**----- Start of picture text -----**<br>
deserialize :deserialize [<va Deserialize specified value/array from JSON or dsv (delimiter separated values)<br>:put [:deserialize<br>lue>] from=[arg] format.<br>from=json value="[\"a\",\"<br>from  specifies the format - json, dsv b\",\"c\"]"]<br>a;b;c<br>delimiter  sets the "separator".<br>:put ([ :deserialize<br>options  specifies additional options: from=dsv delimiter=";"<br>value="a;b;c\n1;findme;3"<br>dsv.plain - deserializes every line as an array (input does not have a<br>options=dsv.plain ]->1-<br>header or column names);<br>>1)<br>dsv.array - expects a header (column names) and will return an array of<br>findme<br>dictionaries, where values are mapped to column names provided in the<br>header.<br>:put ([ :deserialize<br>json.no-string-conversion - prevents implicit conversions from json string<br>from=dsv delimiter=";"<br>type to console values (number, ip, etc.).<br>value="a;b;c\n1;findme;3"<br>options=dsv.plain ]->0-<br>>1)<br>b<br>:put ([:deserialize<br>from=dsv "a;b;c\n1;2;3\n4;<br>5;6" delimiter=";"<br>options=dsv.array]->1->"<br>b")<br>5<br>:put ([:deserialize<br>from=dsv "a;b;c\n1;2;3\n4;<br>5;6" delimiter=";"<br>options=dsv.array]->0->"<br>c")<br>3<br>:put [typeof ([:<br>deserialize "{ \"str\": \"<br>123\" }" from=json<br>options=json.no-string-<br>conversion]->"str")]<br>str<br>:deserialize [/file/get<br>file.json contents]<br>from=json<br>time :time  return interval of time needed to execute the command :put [:time {:for i from=1<br><expression> to=10 do={ :delay 100ms }}];<br>timestamp :timestamp returns the time since epoch, where epoch is January 1, 1970 (Thursday), not<br>[admin@MikroTik] > :put [:<br>counting leap seconds<br>timestamp]<br>2735w21:41:43.481891543<br>or<br>[admin@MikroTik] > :put [:<br>timestamp]<br>2735w1d21:41:43.481891543<br>with the day offset<br>toarray :toarray <var> convert a variable to the array<br>tobool :tobool <var> convert a variable to boolean<br>toid :toid <var> convert a variable to internal ID<br>toip :toip <var> convert a variable to IP address<br>toip6 :toip6 <var> convert a variable to IPv6 address<br>tonum :tonum <var> convert a variable to an integer<br>**----- End of picture text -----**<br>


1104 

**==> picture [509 x 292] intentionally omitted <==**

**----- Start of picture text -----**<br>
tostr :tostr <var> convert a variable to a string<br>totime :totime <var> convert a variable to time<br>tonsec :tonsec <var> convert time to nanoseconds :put [:tonsec value=10:<br>00]<br>36000000000000<br>tocrlf :tocrlf <var> converts line endings to CRLF :put [:tocrlf  "AAA\r\nBBB\r\nCC<br>C"<br>]<br>AAA<br>BBB<br>CCC<br>tolf :tolf <var> converts line endings to LF :put [:tolf  "AAA\nBBB\nCCC"<br>]<br>AAA<br>   BBB<br>      CCC<br>nothing :nothing return a value of nothing :if ([:nothing] = 0) do={:put<br>true} else={:put false}<br>false<br>:if ([:nothing] > 0) do={:put<br>true} else={:put false}<br>false<br>:if ([:nothing] < 0) do={:put<br>true} else={:put false}<br>true<br>**----- End of picture text -----**<br>


**==> picture [13 x 13] intentionally omitted <==**

If a variable type conversion function cannot apply the new format to the provided data, the output will be empty. 

For example, if you run the :tonum <var> command on a variable with a non-integer value such as "23.8" or "cow&chicken", the result will be empty, and its data type will be shown as nil.
