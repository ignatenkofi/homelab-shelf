## Stateful Properties 

662 

**==> picture [516 x 581] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>connection-bytes  ( Matches packets only if a given amount of bytes has been transferred through the particular connection.<br>integer-integer;<br>Default: ) 0 - means infinity, for example connection-bytes=2000000-0 means that the rule matches if more than 2MB has been<br>transferred through the relevant connection<br>connection-limit  (i Matches connections per address or address block after a given value is reached. Should be used together with  connection-<br>nteger,netmask;  state=new  and/or with  tcp-flags=syn  because matcher is very resource-intensive<br>Default: )<br>connection-mark  ( Matches packets marked via mangle facility with particular connection mark. If no-mark is set, the rule will match any unmarked<br>no-mark | string;  connection<br>Default: )<br>connection-nat- Can match connections that are srcnatted, distracted, or both. Note that  connection-state=related  connections connection-<br>state  (srcnat |  nat-state is determined by the direction of the first packet. and if connection tracking needs to use dst-nat to deliver this connection<br>dstnat; Default: ) to the same hosts as the main connection it will be in connection-nat-state=dstnat even if there are no dst-nat rules at all<br>connection-rate  (I Connection Rate is a firewall matcher that allows capturing traffic based on the present speed of the connection<br>nteger 0..<br>4294967295;<br>Default: )<br>connection-state  ( Interprets the connection tracking analytics data for a particular packet:<br>established |<br>invalid | new |  established  - a packet that belongs to an existing connection<br>related |  invalid  - a packet that does not have a determined state in connection tracking (usually - severe out-of-order packets,<br>untracked;  packets with wrong sequence/ack number, or in case of a resource over usage on the router), for this reason, an invalid<br>Default: ) packet will not participate in NAT (as only connection-state=new packets do), and will still contain original source IP address<br>when routed. We strongly suggest dropping all connection-state=invalid packets in the firewall filter forward and input chains<br>new  - the packet has started a new connection or is otherwise associated with a connection that has not seen packets in both<br>directions.<br>related  - a packet that is related to, but not parts of an existing connection, such as ICMP errors or a packet that begins an<br>FTP data connection<br>untracked  - packet that was set to bypass connection tracking in firewall RAW tables.<br>connection-type  (f Matches packets from related connections based on information from their connection tracking helpers. A relevant connection<br>tp | h323 | irc |  helper must be enabled under the:  /ip firewall service-port<br>pptp | quake3 |<br>sip | tftp; Default: )<br>layer7-protocol  (n Layer7 filter name defined in layer7 protocol menu. Read more>>.<br>ame; Default: )<br>p2p  () Matches some unencrypted P2P protocols. Deprecated in modern days since mostly everything is encrypted and requires deep<br>packet inspection to identify.<br>IPv4  only.<br>realm  (integer: 0.. IPv4  only.<br>4294967295;<br>Default: )<br>routing-mark  (string Matches packets marked by mangle facility with particular routing mark<br>; Default: )<br>**----- End of picture text -----**<br>


663
