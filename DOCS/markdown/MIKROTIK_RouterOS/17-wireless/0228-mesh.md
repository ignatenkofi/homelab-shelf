## Mesh 

**==> picture [516 x 397] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>admin-mac  (MAC address;  Administratively assigned MAC address, used when the  auto-mac  setting is disabled<br>Default: 00:00:00:00:00:00)<br>arp  (disabled | enabled |  Address Resolution Protocol setting<br>proxy-arp | reply-only;<br>Default:  enabled )<br>auto-mac  (boolean;  If disabled, then the value from  admin-mac  will be used as the MAC address of the mesh interface; else address of some<br>Default:  no ) port will be used if ports are present<br>hwmp-default-hoplimit  (inte Maximum hop count for generated routing protocol packets; after an HWMP+ packet is forwarded "hoplimit" times, it is<br>ger: 1..255; Default: ) dropped<br>hwmp-prep-lifetime  (time;  Lifetime for routes created from received PREP or PREQ messages<br>Default:  5m )<br>hwmp-preq-destination-only Whether the only destination can respond to HWMP+ PREQ message<br>(boolean; Default:  yes )<br>hwmp-preq-reply-and- Whether intermediate nodes should forward HWMP+ PREQ message after responding to it. Useful only when  hwmp-preq-<br>forward  (boolean; Default:  y destination-only  is disabled<br>es )<br>hwmp-preq-retries  (integer;  How many times to retry a route discovery to a specific MAC address before the address is considered unreachable<br>Default: ) 2<br>hwmp-preq-waiting-time  (ti How long to wait for a response to the first PREQ message. Note that for subsequent PREQs the waiting time is<br>me; Default:  4s ) increased exponentially<br>hwmp-rann-interval  (time;  How often to send out HWMP+ RANN messages<br>Default:  10s )<br>hwmp-rann-lifetime  (time;  Lifetime for routes created from received RANN messages<br>Default:  1s )<br>hwmp-rann-propagation- How long to wait before propagating a RANN message. Value in seconds<br>delay  (number; Default:  0.5 )<br>**----- End of picture text -----**<br>

1492 

mesh-portal (boolean; Whether this interface is a portal in the mesh network Default: no ) mtu (number; Default: 1500 ) Maximum transmission unit size name (string; Default: ) Interface name reoptimize-paths (boolean; Whether to send out periodic PREQ messages asking for known MAC addresses. Turning on this setting is useful if the Default: no ) network topology is changing often. Note that if no reply is received to a re-optimization PREQ, the existing path is kept anyway (until it timeouts itself)
