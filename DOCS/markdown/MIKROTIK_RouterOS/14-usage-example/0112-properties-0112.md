## Properties 

Sub-menu: `/interface traffic-eng` 

**==> picture [506 x 567] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>affinity-exclude  (integer; Default: not  Do not use interface if resource-class matches any of specified bits.<br>set )<br>affinity-include-all  (integer; Default: no Use interface only if resource-class matches all of specified bits.<br>t set )<br>affinity-include-any  (integer; Default: n Use interface if resource-class matches any of specified bits.<br>ot set )<br>auto-bandwidth-avg-interval  (time;  Interval in which actual amount of data is measured, from which average bandwidth is calculated.<br>Default: 5m )<br>auto-bandwidth-range  (Disabled | Min Auto bandwidth adjustment range.  Read more >><br>[bps][-Max[bps]]; Default: 0bps )<br>auto-bandwidth-reserve  (integer[%];  Specifies percentage of additional bandwidth to reserve.  Read more >><br>Default: 0% )<br>auto-bandwidth-update-interval  (time;  Interval during which tunnel keeps track of highest average rate.<br>Default: 1h )<br>bandwidth  (integer[bps]; Default: 0bps ) How much bandwidth to reserve for TE tunnel. Value is in bits per second.  Read more >><br>bandwidth-limit  (disabled | integer[%];  Defines actual bandwidth limitation of TE tunnel. Limit is configured in percent of specified tunnel  bandwidth<br>Default: disabled ) .  Read more >><br>comment  (string; Default: ) Short description of the item<br>disable-running-check  (yes | no;  Specifies whether to detect if interface is running or not. If set to no interface will always have running flag.<br>Default: no )<br>disabled  (yes | no; Default: yes ) Defines whether item is ignored or used.<br>from-address  (auto | IP; Default: auto ) Ingress address of the tunnel. If set to auto least IP address is picked.<br>holding-priority  (integer [0..7]; Default: Is used to decide whether this session can be preempted by another session. 0 sets the highest priority.<br>not set )<br>mtu  (integer; Default: 1500 ) Layer3 Maximum Transmission Unit<br>name  (string; Default: ) Name of the interface<br>primary-path  (string; Default: ) Primary label switching paths defined in /mpls traffic-eng tunnel-path menu.<br>primary-retry-interval  (time; Default: 1m Interval after which tunnel will try to use primary path.<br>)<br>record-route  (yes | no; Default: not set ) If enabled, the sender node will receive information about the actual route that the LSP tunnel traverses.<br>Record Route is analogous to a path vector, and hence can be used for loop detection.<br>reoptimize-interval  (time; Default: not  Interval after which tunnel will re-optimize current path. If current path is not the best path then after<br>set ) optimization best path will be used.  Read more >><br>secondary-paths  (string[,string];  List of label switching paths used by TE tunnel if primary path fails. Paths are defined in  /mpls traffic-<br>Default: ) eng tunnel-path menu.<br>**----- End of picture text -----**<br>


861 

setup-priority (integer[0..7]; Default: n Parameter is used to decide whether this session can preempt another session. 0 sets the highest priority. ot set ) to-address (IP; Default: 0.0.0.0 ) Remote end of TE tunnel.
