## `Move a firewall entry (switch positions):` 

```
curl -k -u <username>:<password> -X POST http://<ip-address>/rest/ip/firewall/nat/move --data '{".id":"*9",".
id":"*C"}' -H "content-type: application/json"
```
