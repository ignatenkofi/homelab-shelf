## Other Useful Commands 

By default print is equivalent to `print static` and shows only static rules. To print also dynamic rules use `print all` . Or to print only dynamic rules use `print dynamic` .
