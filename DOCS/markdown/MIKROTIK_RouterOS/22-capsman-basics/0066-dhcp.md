## DHCP 

```
/ip dhcp-client
add interface=bridge
```

Apply DHCP-client on top of the bridge interface, so that the repeater itself and all its connected clients get IP addresses from the main router. Disable DHCP-server functionality.
