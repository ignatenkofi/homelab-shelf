## Commands 

**==> picture [516 x 109] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>activate  (<partition>) Assigns another partition as Active. This option is available if the "partitions" setting is enabled in device mode (since<br>RouterOS 7.17).<br>repartition  (integer) Will reboot the router and reformat the NAND, leaving only active partition.<br>copy-to  (<partition>) Clone  running  OS with the config to specified partition. Previously stored data on the partition will be erased.<br>save-config-to  (<partition>) Clone  running-config  on a specified partition. Everything else is untouched.<br>**----- End of picture text -----**<br>


1158 

restore-config-from (<partitio Copy config from specified partition to running partition n>)
