## Loops 

**==> picture [509 x 112] intentionally omitted <==**

**----- Start of picture text -----**<br>
Command Syntax Description<br>do..while :do { <commands> } while=( <conditions> ); :while ( <conditions> )  execute commands until a given<br>do={ <commands> }; condition is met.<br>for :for <var> from=<int> to=<int> step=<int> do={ <commands> } execute commands over a given number<br>of iterations<br>foreach :foreach <var> in=<array> do={ <commands> }; execute commands for each element in<br>a list<br>**----- End of picture text -----**<br>
