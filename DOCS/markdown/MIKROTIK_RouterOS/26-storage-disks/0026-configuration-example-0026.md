## Configuration example 

```
/disk add type=ramdisk ramdisk-size=500M
```

**==> picture [13 x 13] intentionally omitted <==**

To start using the created ramdisk, you must first format it. `/disk format ramdisk1` 

1672
