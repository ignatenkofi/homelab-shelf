## Properties 

Sub-menu: `/ip cloud` 

**==> picture [516 x 511] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>ddns-enabled  ( If set to  yes , then the device will send an encrypted message to MikroTik's Cloud server. The server will then decrypt the message<br>yes | auto;  and verify that the sender is an authentic MikroTik device. If all is OK, then MikroTik's Cloud server will create a DDNS record for this<br>Default:  auto ) device and send a response to the device. Every minute the IP/Cloud service on the router will check if the WAN IP address matches<br>the one sent to MikroTik's Cloud server and will send an encrypted update to the cloud server if the IP address changes.<br>If set to auto, ddns will only be enabled if Back To Home is enabled.<br>prior to the 7.17 versions, the default value was "no".<br>ddns-update- If set DDNS will attempt to connect IP Cloud servers at the set interval. If set to  none  it will continue to internally check IP address<br>interval  (time,  update and connect to IP Cloud servers as needed. Useful if the IP address used is not on the router itself and thus, cannot be<br>minimum 60  checked as a value internal to the router.<br>seconds;<br>Default:  none )<br>update-time  (y If set to  yes  then router clock will be set to time, provided by the cloud server  IF  there is no NTP or SNTP client enabled. If set to  no ,<br>es | no;  then IP/Cloud service will never update the device's clock. If update-time is set to  yes , Clock will be updated even when ddns-enabled<br>Default:  yes ) is set to  no .<br>public-address Shows the device's IPv4 address that was sent to the cloud server. This field is visible only after at least one IP Cloud request was<br>(read-only:  successfully completed.<br>address)<br>public- Shows the device's IPv6 address that was sent to the cloud server. This field is visible only after at least one IP Cloud request was<br>address-ivp6  (r successfully completed.<br>ead-only:<br>address)<br>warning  (read- Shows a warning message if the IP address sent by the device differs from the IP address in the UDP packet header as visible by<br>only: string) MikroTik's Cloud server. Typically this happens if the device is behind NAT. Example: "DDNS server received a request from IP<br>123.123.123.123 but your local IP was 192.168.88.23; DDNS service might not work"<br>dns-name  (rea Shows the DNS name assigned to the device. Name consists of 12 characters serial number appended by .sn.mynetname.net. This<br>d-only: name) field is visible only after at least one ddns-request is successfully completed.<br>status  (read- Contains text string that describes the current dns-service state. The messages are self explanatory<br>only: string)<br>updating...<br>updated<br>Error: no Internet connection<br>Error: request timed out<br>Error: REJECTED. Contact MikroTik support<br>Error: internal error  - should not happen. One possible cause is if the router runs out of memory<br>**----- End of picture text -----**<br>
