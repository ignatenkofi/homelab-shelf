## Rule Table 

Rule table is a very powerful tool allowing wire-speed packet filtering, forwarding and VLAN tagging based on L2, L3 and L4 protocol header field conditions. The menu contains an ordered list of rules just like in `/ip firewall filter` , so ACL rules are checked for each packet until a match has been found. If multiple rules can match, then only the first rule will be triggered. A rule without any action parameters is a rule to accept the packet. 

Each rule contains a conditions part and an action part. The action part is controlled by the following parameters: 

**==> picture [516 x 471] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>copy-to-cpu  (no |  Whether to send a packet copy to switch CPU port<br>yes; Default: no )<br>mirror  (no | yes;  Whether to send a packet copy to  mirror-target  port<br>Default: no )<br>new-dst-ports  (na Changes the destination port as specified, multiple ports allowed, including a switch CPU port. An empty setting will drop the<br>me; Default: none ) packet. When the parameter is not used, the packet will be accepted<br>new-vlan-id  (integ Changes the VLAN ID to the specified value or adds a new VLAN tag if one was not already present (the property only applies to<br>er: 0..4095) the  Atheros8316 , and  88E6393X  switch chips ( NOTE : in case of 88E6393X switch chip, vlan-filtering=yes is also required)<br>new-vlan-priority  ( Changes the VLAN priority field (priority code point, the property only applies to  Atheros8327 ,  QCA8337  and  Atheros8316  switch<br>integer: 0..7) chips)<br>rate  (integer: 0.. Sets ingress traffic limitation (bits per second) for matched traffic, can only be applied to the first 32 rule slots (the property only<br>4294967295) applies to  Atheros8327/QCA8337  switch chips)<br>redirect-to-cpu  (n Changes the destination port of a matching packet to the switch CPU<br>o | yes; Default:  no<br>)<br>The conditions part is controlled by the rest of the parameters:<br>Property Description<br>disabled  (no | yes; Default:  no ) Enables or disables switch rule<br>dscp  (integer: 0..63) Matching DSCP field of the packet<br>dst-address  (IP address/Mask) Matching destination IP address and mask<br>dst-address6  (IPv6 address/Mask) Matching destination IPv6 address and mask<br>dst-mac-address  (MAC address/Mask) Matching destination MAC address and mask<br>dst-port  (integer: 0..65535) Matching destination protocol port number or range<br>flow-label  (integer: 0..1048575) Matching IPv6 flow label<br>mac-protocol  (802.2 | arp | capsman | dot1x | homeplug-av | ip | ipv6 | ipx | lacp |  Matching particular MAC protocol specified by protocol name or<br>lldp | loop-protect | macsec | mpls-multicast | mpls-unicast | mvrp | packing-compr |  number (skips VLAN tags if any)<br>packing-simple | pppoe | pppoe-discovery | rarp | romon | service-vlan | vlan | or 0..<br>65535 | or 0x0000-0xffff)<br>**----- End of picture text -----**<br>


487 

**==> picture [516 x 303] intentionally omitted <==**

**----- Start of picture text -----**<br>
ports  (name) Name of the interface on which the rule will apply on the<br>received traffic, multiple ports are allowed. If ports property is left<br>empty, the rule will apply to all switch interfaces<br>protocol  (dccp | ddp | egp | encap | etherip | ggp | gre | hmp | icmp | icmpv6 | idpr- Matching particular IP protocol specified by protocol name or<br>cmtp | igmp | ipencap | ipip | ipsec-ah | ipsec-esp | ipv6 | ipv6-frag | ipv6-nonxt | ipv6- number<br>opts | ipv6-route | iso-tp4 | l2tp | ospf | pim | pup | rdp | rspf | rsvp | sctp | st | tcp |<br>udp | udp-lite | vmtp | vrrp | xns-idp | xtp | or 0..255)<br>src-address  (IP address/Mask) Matching source IP address and mask<br>src-address6  (IPv6 address/Mask) Matching source IPv6 address and mask<br>src-mac-address  (MAC address/Mask) Matching source MAC address and mask<br>src-port  (0..65535) Matching source protocol port number or range<br>switch  (switch group) Matching switch group on which will the rule apply<br>traffic-class  (0..255) Matching IPv6 traffic class<br>vlan-id  (0..4095) Matching VLAN ID (the property only applies to the Atheros8316,<br>Atheros8327, QCA8337, 88E6393X switch chips)<br>vlan-header  (not-present | present) Matching VLAN header, whether the VLAN header is present or<br>not (the property only applies to the Atheros8316, Atheros8327,<br>QCA8337, 88E6393X switch chips. in case of  88E6393X  switch<br>chip, vlan-filtering=yes is also required)<br>vlan-priority  (0..7) Matching VLAN priority (priority code point)<br>**----- End of picture text -----**<br>


**==> picture [13 x 13] intentionally omitted <==**

IPv4 and IPv6 specific conditions cannot be present in the same rule. 

**==> picture [13 x 13] intentionally omitted <==**

Because the rule table is processed entirely in switch chips hardware, there is a limitation to how many rules you may have. Depending on the number of conditions (MAC layer, IP layer, IPv6, L4 layer) you use in your rules, the number of active rules may vary from 8 to 32 for Atheros8316 switch chip, from 8 to 16 for Atheros8327/QCA8337 switch chip and from 42 to 256 for 88E6393X switch chip. You can always do `/ interface ethernet switch rule print` after modifying your rule set to see that no rules at the end of the list are 'invalid' which means those rules did not fit into the switch chip.
