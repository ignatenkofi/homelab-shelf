## Properties 

**==> picture [516 x 557] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>authentication  (pap | chap |  Authentication methods that server will accept.<br>mschap1 | mschap2;<br>Default:  mschap1,mschap2 )<br>default-profile  (name;  default profile to use<br>Default:  default-encryption )<br>enabled  (yes | no; Default:  no Defines whether L2TP server is enabled or not.<br>)<br>max-mru  (integer; Default:  1 Maximum Receive Unit. Max packet size that L2TP interface will be able to receive without packet fragmentation.<br>450 )<br>keepalive-timeout  (integer;  If server during keepalive-timeout period does not receive any packets, it will send keepalive packets every second, five<br>Default:  30 ) times. If the server still does not receive any response from the client, then the client will be disconnected after 5<br>seconds. Logs will show 5x "LCP missed echo reply" messages and then disconnect.<br>max-mtu  (integer; Default:  1 Maximum Transmission Unit. Max packet size that L2TP interface will be able to send without packet fragmentation.<br>450 )<br>use-ipsec  (no | yes | require; When this option is enabled, dynamic IPSec peer configuration is added to suite most of the L2TP road-warrior setups.<br>Default:  no ) When require is selected server will accept only those L2TP connection attempts that were encapsulated in the IPSec<br>tunnel.<br>ipsec-secret  (string;  Preshared key used when use-ipsec is enabled<br>Default: )<br>accept-proto-version  ( all  |  Specify protocol version.<br>l2tpv2  l2tpv3; Default: |  all  )<br>cli-only<br>accept-pseudowire-type  (  Set the pseudowire signaling protocol for specific pseudowire type.<br>all ether ppp; Default: |  |  all  )<br>allow-fast-path  (no | yes;  To forward packets without additional processing in the Linux kernel.<br>Default:  no  )<br>caller-id-type  ( ip-address  | If same source IP address is used for multiple clients set id type to number.<br>number; Default:  ip-address )<br>max-sessions  ( unlimited /  Set number of needed sessions.<br>number; Default:  unlimited  )<br>one-session-per-host   ( no |  To allow one session per host.<br>yes | ; Default:  no  )<br>l2tpv3-circuit-id  (Default: ) Set the virtual circuit identifier to bind the one end of the L2TPv3 control channel.<br>l2tpv3-cookie-length  (0 4-|  Configures an L2TP pseudowire static session cookie.<br>bytes 8-bytes; Default: |  0  )<br>l2tpv3-digest-hash  ( md5 n|  Specifies which hash function to be used.<br>one sha1; Default: |  md5  )<br>**----- End of picture text -----**<br>


1238 

l2tpv3-ether-interface-list Set your interface list for example the default ones- all, dynamic, none, static. (Default: ) mrru (disabled | integer; Maximum packet size that can be received on the link. If a packet is bigger than tunnel MTU, it will be split into multiple Default: disabled ) packets, allowing full size IP or Ethernet packets to be sent over the tunnel.
