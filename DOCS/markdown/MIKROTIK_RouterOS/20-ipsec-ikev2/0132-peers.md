## Peers 

**==> picture [516 x 630] intentionally omitted <==**

**----- Start of picture text -----**<br>
Property Description<br>allowed- List of IP (v4 or v6) addresses with CIDR masks from which incoming traffic for this peer is allowed and to which outgoing traffic for this<br>address  (I peer is directed. This IP address has to be in the same subnet as WireGuard interface set on ROS. If WireGuard interface is at<br>P/IPv6  192.168.99.1/24, You have to input 192.168.99.2 to the client. By adding this IP under 'Allowed Address', you are saying that only this<br>prefix; Def specific client (phone for example) is permitted to connect to this peer configuration. Allowed-address range cannot overlap on one<br>ault: ) interface, so you need to set own range for each peer.<br>comment  ( Short description of the peer.<br>string;<br>Default: )<br>disabled  ( Enables/disables the peer.<br>yes | no;<br>Default:  no<br>)<br>endpoint- The IP address or hostname. It is used by WireGuard to establish a secure connection between two peers.<br>address  (I<br>P<br>/Hostnam<br>e;<br>Default: )<br>endpoint- The Endpoint port is the UDP port on which a WireGuard peer listens for incoming traffic.<br>port  (integ<br>er:0..<br>65535;<br>Default: )<br>interface  ( Name of the WireGuard interface the peer belongs to.<br>string;<br>Default: )<br>persistent- A seconds interval, between 1 and 65535 inclusive, of how often to send an authenticated empty packet to the peer for the purpose of<br>keepalive keeping a stateful firewall or NAT mapping valid persistently. For example, if the interface very rarely sends traffic, but it might at anytime<br>(integer:0.. receive traffic from a peer, and it is behind NAT, the interface might benefit from having a persistent keepalive interval of 25 seconds.<br>65535;<br>Default: 0)<br>preshared A base64 preshared key. Optional, and may be omitted. This option adds an additional layer of symmetric-key cryptography to be mixed<br>-key  (strin into the already existing public-key cryptography, for post-quantum resistance. Also can be generated automatically or entered manually,<br>g;  when the key is provided by the system administrator.<br>Default: )<br>private- A base64 private key.<br>key  (auto<br>/none;<br>Default:  n<br>one )<br>public-key  ( A base64 public key is calculated from the private key. Each peer has a public key. Public keys are used by peers to authenticate each<br>string;  other. They can be passed around for use in configuration files.<br>Default: )<br>show-  Will show already created Peer configuration and generate a QR code for easier peer setup on a client device. Does not affect the<br>client- WireGuard Server. To view QR code, show-sensitive is required from 7.21_ab548.<br>config<br>Used for the client-server setup scenario, when the configuration is imported using a qr code for a client, configuration details on tab with qrcode will<br>appear once it has been set in the fields:<br>**----- End of picture text -----**<br>


1269 

**==> picture [516 x 387] intentionally omitted <==**

**----- Start of picture text -----**<br>
client- When imported using a qr code for a client (for example, a phone), then this address for the wg interface is set on that device.<br>address  (I<br>P/IPv6<br>prefix;<br>Default: )<br>client-dns  Specify when using WireGuard Server as a VPN gateway for peer traffic.<br>(IP/IPv6<br>prefix;<br>Default: )<br>client- The IP address and port number of the WireGuard Server.<br>endpoint  (I<br>P/IPv6<br>prefix;<br>Default: )<br>client- Same as  persistent-keepalive  but from peer side.<br>keepalive  (<br>integer:0..<br>65535;<br>Default: 0)<br>client- The local port upon which this WireGuard tunnel will listen for incoming traffic from peers, and the port from which it will source outgoing<br>listen-port  ( packets.<br>integer:0..<br>65535;<br>Default: )<br>name  (stri Allows adding name to a peer. Name will be used as a reference for a peer in WireGuard logs. (Available from RouterOS version 7.15)<br>ng;<br>Default: )<br>responder  Specifies if peer is intended to be connection initiator or only responder. Should be used on WireGuard devices that are used as "servers"<br>(yes | no;  for other devices as clients to connect to. Otherwise router will all repeatedly try to connect "endpoint-address" or "current-endpoint-<br>Default:  no address".<br>)<br>**----- End of picture text -----**<br>


*AllowedIPs configuration that is provided to the client through WireGuard peer export (configuration file or QR code) can not be changed and will be "0.0.0.0/0, ::/0" at the moment. If it is necessary to change these values on remote end, then that is up to the remote peer software used for WireGuard connection.
