## Properties 

**==> picture [516 x 397] intentionally omitted <==**

**----- Start of picture text -----**<br>
arp  (disabled |  Address Resolution Protocol mode:<br>enabled | proxy-arp |<br>reply-only; Default:  e disabled - the interface will not use ARP<br>nabled ) enabled - the interface will use ARP<br>proxy-arp - the interface will use the ARP proxy feature<br>reply-only - the interface will only reply to requests originating from matching IP address/MAC address combinations which<br>are entered as static entries in the "/ip arp" table. No dynamic entries will be automatically stored in the ARP table.<br>Therefore for communications to be successful, a valid static entry must already exist.<br>bandwidth  (integer Sets max rx/tx bandwidth in kbps that will be handled by an interface. TX limit is supported on all Atheros switch-chip ports. RX<br>/integer; Default:  unli limit is supported only on Atheros8327/QCA8337 switch-chip ports.<br>mited/unlimited )<br>comment  (string;  Descriptive name of an item<br>Default: )<br>l2mtu  (integer [0.. Layer2 Maximum transmission unit. MTU in RouterOS<br>65536]; Default: )<br>mac-address  (MAC;  Media Access Control number of an interface.<br>Default: )<br>mtu  (integer [0.. Layer3 Maximum transmission unit<br>65536]; Default:  1500 )<br>name  (string;  Name of an interface<br>Default: )<br>orig-mac-address  (M<br>AC; Default: )<br>rx-flow-control  (on |  When set to on, the port will process received pause frames and suspend transmission if required.  auto  is the same as  on<br>off | auto; Default:  off ) except when auto-negotiation=yes flow control status is resolved by taking into account what the other end advertises. The<br>feature is supported on AR724x, AR9xxx, and QCA9xxx CPU ports, all CCR ports, and all Atheros switch chip ports.<br>tx-flow-control  (on |  When set to on, the port will send pause frames when specific buffer usage thresholds are met.  auto  is the same as  on  except<br>off | auto; Default:  off ) when auto-negotiation=yes flow control status is resolved by taking into account what the other end advertises. The feature is<br>supported on AR724x, AR9xxx, and QCA9xxx CPU ports, all CCR ports, and all Atheros switch chip ports.<br>**----- End of picture text -----**<br>
