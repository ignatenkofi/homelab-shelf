## Properties 

Property Description 

172 

**==> picture [506 x 414] intentionally omitted <==**

**----- Start of picture text -----**<br>
6to4- If this option is specified, this prefix will be combined with the IPv4 address of the interface name to produce a valid 6to4 prefix. The<br>interface  (no first 16 bits of this prefix will be replaced by 2002 and the next 32 bits of this prefix will be replaced by the IPv4 address assigned to<br>ne | string;  the interface name at configuration time. The remaining 80 bits of the prefix (including the SLA ID) will be advertised as specified in<br>Default: ) the configuration file.<br>autonomous When set, indicates that this prefix can be used for autonomous address configuration. Otherwise, prefix information is silently<br>(yes | no;  ignored.<br>Default: yes )<br>comment  (st Descriptive name of an item<br>ring;<br>Default: )<br>disabled  (ye Whether an item is disabled or not. By default, entry is enabled.<br>s | no;<br>Default: no )<br>on-link  (yes  When set, indicates that this prefix can be used for on-link determination. When not set the advertisement makes no statement about<br>| no; Default: the on-link or off-link properties of the prefix. For instance, the prefix might be used for address configuration with some of the<br>yes ) addresses belonging to the prefix being on-link and others being off-link.<br>preferred- Timeframe (relative to the time the packet is sent) after which generated address becomes "deprecated". Deprecated is used only for<br>lifetime  (infin already existing connections and is usable until valid lifetime expires. Read more >><br>ity | time;<br>Default: 1w )<br>prefix  (ipv6  A prefix from which stateless address auto-configuration generates the valid address. If option "none" is selected, then only options<br>prefix  none| will be advertised without including specific prefix.<br>; Default:  ::<br>/64 )<br>valid-lifetime The length of time (relative to the time the packet is sent) an address remains in the valid state. The valid lifetime must be greater<br>(infinity |  than or equal to the preferred lifetime  . Read more >><br>time;<br>Default:  4w2d<br>)<br>interface  (str Interface name on which stateless auto-configuration will be running.<br>ing; Default:<br>)<br>**----- End of picture text -----**<br>
