## 5.2 PSK Example 

Generate one-time PSKs for PPK use: 

`/ip ipsec key psk generate count=10 key-size=32` count → number of PSKs to generate. key-size → size of each key in bytes (32 bytes = 256 bits).
